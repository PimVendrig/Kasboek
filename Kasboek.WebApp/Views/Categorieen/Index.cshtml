@model IEnumerable<Kasboek.WebApp.Models.Categorie>

@{
    ViewData["Title"] = "Categorieën";
}

<h2>Categorieën</h2>

<p>
    <a asp-action="Create">Categorie maken</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Omschrijving)
                </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Details" asp-route-id="@item.CategorieId">@Html.DisplayFor(modelItem => item.Omschrijving)</a>
            </td>
        </tr>
}
    </tbody>
</table>
