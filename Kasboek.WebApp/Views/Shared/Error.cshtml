@using Kasboek.WebApp.Models.SharedViewModels
@model ErrorViewModel
@{
    ViewData["Title"] = "Fout";
}

<h3 class="text-danger">Er is een fout opgetreden bij het verwerken van uw verzoek.</h3>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Verzoek-ID:</strong> <code>@Model.RequestId</code>
    </p>
}
